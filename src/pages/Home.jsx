import React from 'react';
import styles from '../styles/padding.module.css';

const Home = () => {
  return <main className={` ${styles.headerPadding} h-screen dark:bg-neutral-very-dark-blue-bg`}>Home</main>;
};

export default Home;
